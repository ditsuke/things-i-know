`gq` might not work on a configured [[neovim]] instance. Why? It's likely because either `formatexpr` or `formatprog` are set.

References:
1. https://www.reddit.com/r/neovim/comments/zalqgc/gq_and_formatexpr_weirdness/
